# Light runtime: everything in this file is enough to run the tests, the API and
# the demo config (fake embedder + in-memory store). No GPU, no downloads.
# Heavy ML/vector-DB packages live in requirements-ml.txt. Python 3.11+.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
numpy>=1.26
pyyaml>=6.0
python-multipart>=0.0.9
pytest>=8.0
httpx>=0.27
# Sprint 5 demo UI (app/streamlit_app.py); it only speaks HTTP to app/api.py.
streamlit>=1.40
